<!DOCTYPE html>
<html>
  <head>
    <title>DwBlogV2</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <!--
      This comment is here to include classes for Tailwind's purge process.
      pagy nav pagy.nav current disabled
    -->
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100">
    <%= render 'layouts/navbar' %>

    <div class="fixed top-8 left-1/2 transform -translate-x-1/2 z-50">
      <% flash.each do |key, message| %>
        <div data-controller="flash" class="flash-message <%= key %>">
          <p class="text-sm"><%= message %></p>
        </div>
      <% end %>
    </div>

    <main class="container mx-auto mt-8 px-5">
      <%= yield %>
    </main>

    <%= render 'layouts/footer' %>
  </body>
</html>
