<div class="p-8">
  <h1 class="font-bold text-4xl mb-6">Tags</h1>

  <div class="mb-4">
    <%= link_to 'Add New Tag', new_admin_tag_path, class: 'btn-primary' %>
  </div>

  <div class="overflow-x-auto rounded-lg shadow-md">
    <table class="min-w-full bg-white">
      <thead>
        <tr>
          <th class="table-header">Name</th>
          <th class="table-header">Actions</th>
        </tr>
      </thead>
      <tbody class="text-gray-700">
        <% @tags.each do |tag| %>
          <tr class="hover:bg-gray-100">
            <td class="table-cell"><%= tag.name %></td>
            <td class="table-cell">
              <div class="flex space-x-2">
                <%= link_to edit_admin_tag_path(tag), class: "btn-secondary" do %>
                  <%= image_tag('edit-white.svg', alt: 'Edit', class: 'w-4 h-4') %>
                <% end %>
                <%= button_to admin_tag_path(tag), method: :delete, data: { confirm: 'Are you sure?' }, class: "btn-danger" do %>
                  <%= image_tag('cross-white.svg', alt: 'Delete', class: 'w-4 h-4') %>
                <% end %>
              </div>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>